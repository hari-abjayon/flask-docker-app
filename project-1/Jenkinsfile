pipeline {
    agent any
 
    environment {
        IMAGE_NAME = "hariabjayon15/flask-app"
    }
 
    stages {
        stage('Checkout Code') {
            steps {
                script {
                    // Print the workspace path
                    echo "Workspace directory: ${env.WORKSPACE}"
                    
                    // Verify project directory after checkout
                    sh "ls -la ${env.WORKSPACE}"
                }
            }
        }
 
        stage('Build Docker Image') {
            steps {
                script {
                    def BUILD_NUMBER = env.BUILD_NUMBER
                    def WORKDIR = "${env.WORKSPACE}/project-1".replace("\\", "/") // Ensure proper path format
                    
                    echo "Checking project directory at: ${WORKDIR}"
                    
                    // Verify project directory exists
                    sh "ls -la ${WORKDIR} || echo 'Project directory missing!'"
 
                    // Build and tag the Docker image
                    sh "docker build -t ${IMAGE_NAME}:${BUILD_NUMBER} ${WORKDIR}"
                    sh "docker tag ${IMAGE_NAME}:${BUILD_NUMBER} ${IMAGE_NAME}:latest"
                }
            }
        }
 
        stage('Push to Docker Hub') {
            steps {
                script {
                    def BUILD_NUMBER = env.BUILD_NUMBER
 
                    // Authenticate with Docker Hub
                    sh "echo ${DOCKER_HUB_PASSWORD} | docker login -u ${DOCKER_HUB_USERNAME} --password-stdin"
 
                    // Push images to Docker Hub
                    sh "docker push ${IMAGE_NAME}:${BUILD_NUMBER}"
                    sh "docker push ${IMAGE_NAME}:latest"
                }
            }
        }
    }
 
    post {
        failure {
            echo "Build failed. Please check the logs."
        }
        success {
            echo "Build and push to Docker Hub completed successfully."
        }
    }
}
 