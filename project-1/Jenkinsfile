pipeline {
    agent any

    environment {
        IMAGE_NAME = "hariabjayon15/flask-app"
        BUILD_TAG = "${BUILD_NUMBER}"
    }

    stages {
        stage('Checkout Code') {
            steps {
                script {
                    echo "Workspace directory: ${env.WORKSPACE}"
                    echo "Checking for project directory: ${env.WORKSPACE}\\project-1"
                    if (!fileExists("${env.WORKSPACE}\\project-1")) {
                        error "ERROR: Project directory missing!"
                    }
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    echo "Building Docker image..."
                    dir('project-1') {
                        powershell """
                        docker build -t ${IMAGE_NAME}:${BUILD_TAG} .
                        docker tag ${IMAGE_NAME}:${BUILD_TAG} ${IMAGE_NAME}:latest
                        """
                    }
                }
            }
        }

        stage('Login to Docker Hub') {
            steps {
                script {
                    withCredentials([string(credentialsId: 'docker-hub-token', variable: 'DOCKER_PASS')]) {
                        powershell """
                        docker login -u hariabjayon15 --password-stdin <<< \$DOCKER_PASS
                        """
                    }
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                script {
                    powershell """
                    docker push ${IMAGE_NAME}:${BUILD_TAG}
                    docker push ${IMAGE_NAME}:latest
                    """
                }
            }
        }
    }

    post {
        failure {
            echo "❌ Build failed. Please check the logs."
        }
    }
}

 